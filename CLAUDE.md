# SBA — מערכת ניהול החזרות

## מה המערכת עושה

מערכת S.B.A. לניהול החזרות לקוח נועדה לרכז את כל תהליך החזרת ציוד התבניות —
משלב תיאום ההחזרה ועד סגירת התיק — במערכת אחת, ולהחליף את התקשורת המפוזרת הנוכחית
(WhatsApp, מייל, Outlook, Excel, טפסים פיזיים).

**הבעיה:** פערים בין S.B.A. ללקוח מתגלים כ-4 פעמים בחודש ומובילים לסכסוכים כספיים —
כ-5 פרויקטים בשנה, עלות כוללת ~1,450,000 ₪ לשנה.

**הפתרון:** שלוש שכבות —
1. מערכת פנימית לניהול תיקי החזרה (עובדי S.B.A.)
2. בוט WhatsApp כנקודת כניסה ללקוח החיצוני
3. ממשקי אינטרנט ייעודיים דרך קישורים חד-פעמיים (24 שעות): חתימה דיגיטלית, תיאום, העלאת מסמכים, ביטול

**משתמשים:** מתאמת החזרות (×2) · מנהלת לוגיסטיקה · מנהלת מפעל · לקוח חיצוני

## ארכיטקטורה וטכנולוגיה

| שכבה | טכנולוגיה |
|------|-----------|
| Frontend | React + Tailwind CSS + shadcn/ui (RTL עברית מלאה) |
| Backend | Supabase Edge Functions |
| Database | PostgreSQL (Supabase Cloud) |
| Storage | Supabase Storage (קבצים מוצפנים at-rest) |
| Deploy | Vercel (UI) + Supabase Cloud (backend) |
| Auth | Supabase Auth — RBAC, 4 תפקידים, קישורים חד-פעמיים |

**ארכיטקטורה:** Modular Monolith — 8 מודולים:
`cases` · `customers` · `documents` · `coordination` · `notifications` · `whatsapp` · `auth` · `search`

**סביבות:** `dev` → `staging` → `production`
(deploy ל-production דורש אישור ידני; כל סביבה — Supabase project + Vercel deployment נפרדים)

## מבנה תיקיות

```
Return-management/
├── CLAUDE.md
└── docs/
    ├── architecture.md       # ארכיטקטורה v1.0 — החלטות טכניות מאושרות
    └── epics-and-stories.md  # אפיקים וסיפורי משתמש v1.2 — 7 אפיקים, 41 סיפורים
```

## מצב הפרויקט

**סטטוס:** מוכן לפיתוח — כל המסמכים מאושרים, טרם נכתבה שורת קוד.

**סיפור נוכחי:** —

**הושלם עד כה:**
- [x] PRD v1.1 — דרישות פונקציונליות (FR1–FR25) ו-NFR (NFR1–NFR13) מאושרות
- [x] ארכיטקטורה v1.0 — 10 החלטות טכניות מאושרות
- [x] Epics & Stories v1.2 — 7 אפיקים, 41 סיפורים, כיסוי מלא של כל NFR
